import { Spinner } from "@labanaat/ui/spinner";

export const metadata = {
  title: "Spinner",
  description: "Loading indicator with role=\"status\", used internally by Button's isLoading state.",
};

# Spinner

Loading indicator with `role="status"`, used internally by `Button`'s `isLoading` state.

<LiveDemo code={`<Spinner />`} />

## Sizes

<LiveDemo code={`<div className="ui-flex ui-items-center ui-gap-3">
    <Spinner size="sm" />
    <Spinner size="md" />
    <Spinner size="lg" />
  </div>`} />

## Installation

```tsx
import { Spinner } from "@labanaat/ui/spinner";
```

## Props

<PropsTable rows={[
  { name: "size", type: '"sm" | "md" | "lg"', default: '"md"', description: "Size scale" },
  { name: "label", type: "string", description: "Accessible label announced to screen readers; defaults to a localized string" },
]} />
