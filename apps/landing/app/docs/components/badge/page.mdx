import { Badge } from "@labanaat/ui/badge";

export const metadata = {
  title: "Badge",
  description: "Small status or labeling element with six semantic variants.",
};

# Badge

Small status or labeling element with six semantic variants.

<LiveDemo code={`<Badge>New</Badge>`} />

## Variants

<LiveDemo code={`<div className="ui-flex ui-flex-wrap ui-gap-2">
    <Badge variant="neutral">Neutral</Badge>
    <Badge variant="primary">Primary</Badge>
    <Badge variant="success">Success</Badge>
    <Badge variant="warning">Warning</Badge>
    <Badge variant="danger">Danger</Badge>
    <Badge variant="outline">Outline</Badge>
  </div>`} />

## Installation

```tsx
import { Badge } from "@labanaat/ui/badge";
```

## Props

<PropsTable rows={[
  { name: "variant", type: '"neutral" | "primary" | "success" | "warning" | "danger" | "outline"', default: '"neutral"', description: "Visual style" },
]} />
