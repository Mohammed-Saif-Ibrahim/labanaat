# Installation

```bash
npm install @labanaat/ui
```

Import the base stylesheet once, at your app root:

```tsx
import "@labanaat/ui/styles.css";
```

Then import components from their individual entry points for optimal
tree-shaking:

```tsx
import { Button } from "@labanaat/ui/button";
```
